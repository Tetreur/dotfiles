- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc

- create:
    - ~/WORK

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
