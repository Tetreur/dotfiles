- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zshrc:

- create:
    - ~/WORK

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
    - echo "heyyyyyy"
    - sleep 2
    -
      command: ./test.zsh
      stdout: true
      stderr: true
