- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zshrc:

- create:
    - ~/WORK

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
    - echo "heyyyyyy"
    - sleep 2
    -
        command: ./setup_with_homebrew.zsh
        stdout: true
        stderr: true
      stdout: true
      stderr: true
